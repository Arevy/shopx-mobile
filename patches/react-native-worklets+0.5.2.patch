diff --git a/node_modules/react-native-worklets/android/CMakeLists.txt b/node_modules/react-native-worklets/android/CMakeLists.txt
index 1d949d6..fed1c23 100644
--- a/node_modules/react-native-worklets/android/CMakeLists.txt
+++ b/node_modules/react-native-worklets/android/CMakeLists.txt
@@ -105,7 +105,13 @@ else()
 endif()
 
 if(${JS_RUNTIME} STREQUAL "hermes")
-  target_link_libraries(worklets hermes-engine::libhermes)
+  if(TARGET hermes-engine::libhermes)
+    target_link_libraries(worklets hermes-engine::libhermes)
+  elseif(TARGET hermes-engine::hermesvm)
+    target_link_libraries(worklets hermes-engine::hermesvm)
+  else()
+    message(FATAL_ERROR "Hermes engine target not found")
+  endif()
 
   if(${HERMES_ENABLE_DEBUGGER})
     string(APPEND CMAKE_CXX_FLAGS " -DHERMES_ENABLE_DEBUGGER=1")
diff --git a/node_modules/react-native-worklets/compatibility.json b/node_modules/react-native-worklets/compatibility.json
index 523807f..22faf6b 100644
--- a/node_modules/react-native-worklets/compatibility.json
+++ b/node_modules/react-native-worklets/compatibility.json
@@ -1,11 +1,29 @@
 {
   "nightly": {
-    "react-native": ["0.78", "0.79", "0.80", "0.81"]
+    "react-native": [
+      "0.78",
+      "0.79",
+      "0.80",
+      "0.81",
+      "0.82"
+    ]
   },
   "0.5.x": {
-    "react-native": ["0.78", "0.79", "0.80", "0.81"]
+    "react-native": [
+      "0.78",
+      "0.79",
+      "0.80",
+      "0.81",
+      "0.82"
+    ]
   },
   "0.4.x": {
-    "react-native": ["0.78", "0.79", "0.80", "0.81"]
+    "react-native": [
+      "0.78",
+      "0.79",
+      "0.80",
+      "0.81",
+      "0.82"
+    ]
   }
 }
