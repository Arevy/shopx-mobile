# GraphQL API exposed by the ShopX backend (update for your environment)
GRAPHQL_ENDPOINT=http://localhost:4000/graphql

# Optional CDN/domain override for product and content assets
IMAGE_CDN_URL=http://localhost:4000/uploads

# Shared presentation settings aligned with the web frontend
SITE_NAME=ShopX

# Server-side services proxy configuration
SERVER_SERVICES_BASE_PATH=/api/serverSideServices
USE_SERVER_SERVICES=false
SERVER_SERVICES_TOKEN=development
